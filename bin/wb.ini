APP_NAME = webhook-basket

[path]
CLONED_REPO_ROOT = repos ; Temporary root for cloned repositories
DEPLOYMENT_ROOT = /home/my-website ; Root on remote server for deployment

[git]
USERNAME = username
PASSWORD = password

[ftp]
HOST = localhost
PORT = 21
USERNAME = anonymous
PASSWORD = anonymous
; PASSIVE = true ; For FTP only, not for SFTP
